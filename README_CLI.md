# CLI Demo

Basically demo script without gradio UI.
Using the CLI script you can inspect the progress bar during longer runs.

```bash
python3 demo_cli.py --model_name MASt3R_ViTLarge_BaseDecoder_512_catmlpdpt_metric --images /photos --output /output/model --tmp_dir /output/tmp
```
